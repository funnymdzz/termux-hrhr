name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: termux/package-builder

    steps:
    - name: termux
      run: ls /home/builder/ && /home/builder/termux-packages/build-package.sh bash
