name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: termux
      run: sudo git clone https://github.com/termux/termux-packages --depth=1 && cd termux-packages && sudo ./build-package.sh bash
