name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: termux/package-builder

    steps:
    - name: termux
      run: ls /home && cd /home/builder/termux-packages/ && ls && ./build-package.sh bash
